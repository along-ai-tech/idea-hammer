{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ExampleSkillInput",
  "description": "示例 skill 的输入契约。复制本文件改 title / required / properties 即得新 schema。",
  "type": "object",
  "required": ["user_goal"],
  "properties": {
    "user_goal": {
      "type": "string",
      "description": "用户原始目标描述（一两句话）",
      "minLength": 5
    },
    "context_files": {
      "type": "array",
      "description": "可选：项目内已有文档路径列表，用于辅助理解",
      "items": {
        "type": "string"
      }
    },
    "constraints": {
      "type": "object",
      "description": "可选：用户给出的硬约束（技术栈、预算、时间）",
      "properties": {
        "tech_stack": { "type": "string" },
        "deadline": { "type": "string", "format": "date" }
      }
    }
  },
  "additionalProperties": false
}
